import styled from '@emotion/styled';

export const Ul = styled.ul`
  display: flex;
  flex-direction: row;
  gap: 12px;
  flex-wrap: wrap;
  justify-content: space-around;

  margin: 0 auto;
  padding: 0 18px;
`;
